<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ss.mapper.StudentMapper">
    <insert id="insert">
        insert into student (name, no, gender, phone, id_card, is_college, address, degree, graduation_date, clazz_id, create_time, update_time)
        values (#{name}, #{no}, #{gender}, #{phone}, #{idCard}, #{isCollege}, #{address}, #{degree}, #{graduationDate}, #{clazzId}, #{createTime}, #{updateTime})
    </insert>
    <update id="update">
        update student
        <set>
            <if test="name != null and name != ''">name = #{name},</if>
            <if test="no != null and no != ''">no = #{no},</if>
            <if test="gender != null">gender = #{gender},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="idCard != null and idCard != ''">id_card = #{idCard},</if>
            <if test="isCollege != null ">is_college = #{isCollege},</if>
            <if test="address != null and address != ''">address = #{address},</if>
            <if test="degree != null">degree = #{degree},</if>
            <if test="graduationDate != null">graduation_date = #{graduationDate},</if>
            <if test="clazzId != null">clazz_id = #{clazzId},</if>
            update_time = #{updateTime}
        </set>
        where id = #{id}
    </update>
    <update id="updateViolationCount">
        update student set violation_count = violation_count + 1 where id = #{id}
    </update>
    <update id="updateViolationScore">
        update student set violation_score = violation_score + #{score} where id = #{id}
    </update>
    <delete id="delete">
        delete from student where id in
        <foreach item="id" collection="ids" separator="," open="(" close=")">
            #{id}
        </foreach>
    </delete>
    <select id="list" resultType="com.ss.pojo.Student">
        SELECT s.*, c.name clazzName FROM student s left join clazz c on s.clazz_id = c.id
    </select>
    <select id="selectById" resultType="com.ss.pojo.Student">
        select * from student where id = #{id}
    </select>
    <select id="countStu" resultType="java.util.Map">
        select
            (case clazz_id
                when 1 then 'JavaEE就业163期'
                when 2 then '前端就业90期'
                when 3 then 'JavaEE就业165期'
                when 4 then 'JavaEE就业166期'
                when 5 then '大数据就业58期'
                when 6 then 'JavaEE就业167期'
                else '其他' end) pos,
        count(*) num
        from student group by clazz_id order by num;
    </select>
    <select id="countStuDegreeData" resultType="java.util.Map">
        select
            (case degree
                when 1 then '初中'
                when 2 then '高中'
                when 3  then '大专'
                when 4  then '本科'
                when 5  then '硕士'
                when 6  then '博士' end) name,
        count(*) value
        from student group by degree;
    </select>
</mapper>